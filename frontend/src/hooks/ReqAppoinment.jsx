import { useEffect } from "react";
import axios from "axios";
import { BASE_URL, token } from "../config";
const ReqAppoinment = ({ details }) => {
  useEffect(() => {}, []);
};

export default ReqAppoinment;
